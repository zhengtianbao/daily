import { View } from 'react-native';
import { Text } from 'react-native-paper';

const BooksScreen = () => {
  return (
    <View>
      <Text>Books</Text>
    </View>
  );
};

export default BooksScreen;
